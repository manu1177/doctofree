{% extends 'base.html.twig' %}

{% block title %}Edit Medicament{% endblock %}

{% block body %}
    <h1>Edit Medicament</h1>

    {{ include('admin/medicament/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_admin_medicament_index') }}">back to list</a>

    {{ include('admin/medicament/_delete_form.html.twig') }}
{% endblock %}
